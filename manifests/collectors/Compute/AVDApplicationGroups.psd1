#
# GENERATED by scripts/ConvertTo-ScoutCollectorDefinition.ps1 from Modules/Public/InventoryModules/Compute/AVDApplicationGroups.ps1 (AB#5660).
# Field expressions are copied verbatim from the original collector and evaluate in an
# equivalent scope -- see docs/design/decisions/declarative-collectors.md.
# Review before trusting; regenerate rather than hand-patch if the source collector changes.
#
@{
    ResourceTypes = @(
        'microsoft.desktopvirtualization/applicationgroups'
    )

    ResourceTypeMatching = 'Grouped'

    AdditionalFilter = $null

    FilterPreamble = ''

    RowLoopVariable = '1'

    Preamble = @'
$ResUCount = 1
            $sub1 = $SUB | Where-Object { $_.Id -eq $1.subscriptionId }
            $data = $1.PROPERTIES
            $Tags = if (![string]::IsNullOrEmpty($1.tags.psobject.properties)) { $1.tags.psobject.properties } else { '0' }

            $hostPoolName = if ($data.hostPoolArmPath) { ($data.hostPoolArmPath -split '/')[-1] } else { 'N/A' }
            $workspaceName = if ($data.workspaceArmPath) { ($data.workspaceArmPath -split '/')[-1] } else { 'N/A' }
'@

    AdditionalRowLoops = @()

    TagLoop = @{
        Variable = 'Tag'
        Source = '$Tags'
        Preamble = ''
    }

    Fields = @(
        @{
            Name = 'ID'
            Expression = '$1.id'
        }
        @{
            Name = 'Subscription'
            Expression = '$sub1.Name'
        }
        @{
            Name = 'Resource Group'
            Expression = '$1.RESOURCEGROUP'
        }
        @{
            Name = 'Name'
            Expression = '$1.NAME'
        }
        @{
            Name = 'Location'
            Expression = '$1.LOCATION'
        }
        @{
            Name = 'Friendly Name'
            Expression = 'if ($data.friendlyName)           { $data.friendlyName }           else { ''N/A'' }'
        }
        @{
            Name = 'Description'
            Expression = 'if ($data.description)            { $data.description }            else { ''N/A'' }'
        }
        @{
            Name = 'Application Group Type'
            Expression = 'if ($data.applicationGroupType)   { $data.applicationGroupType }   else { ''N/A'' }'
        }
        @{
            Name = 'Host Pool'
            Expression = '$hostPoolName'
        }
        @{
            Name = 'Workspace'
            Expression = '$workspaceName'
        }
        @{
            Name = 'Provisioning State'
            Expression = 'if ($data.provisioningState)      { $data.provisioningState }      else { ''N/A'' }'
        }
        @{
            Name = 'Resource U'
            Expression = '$ResUCount'
        }
        @{
            Name = 'Tag Name'
            Expression = '[string]$Tag.Name'
        }
        @{
            Name = 'Tag Value'
            Expression = '[string]$Tag.Value'
        }
    )

    Export = @{
        WorksheetName = 'AVD Application Groups'
        TableNamePrefix = 'AVDAppGroupsTable_'
        Columns = @(
            'Subscription'
            'Resource Group'
            'Name'
            'Location'
            'Friendly Name'
            'Description'
            'Application Group Type'
            'Host Pool'
            'Workspace'
            'Provisioning State'
            'Resource U'
        )
        TagColumns = @(
            'Tag Name'
            'Tag Value'
        )
        TagColumnsBefore = $null
        NumberFormat = '0'
        ConditionalText = @()
    }

    SourceCollector = 'Modules/Public/InventoryModules/Compute/AVDApplicationGroups.ps1'
}

