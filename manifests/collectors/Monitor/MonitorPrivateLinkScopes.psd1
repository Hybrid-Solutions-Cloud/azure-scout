#
# GENERATED by scripts/ConvertTo-ScoutCollectorDefinition.ps1 from Modules/Public/InventoryModules/Monitor/MonitorPrivateLinkScopes.ps1 (AB#5660).
# Field expressions are copied verbatim from the original collector and evaluate in an
# equivalent scope -- see docs/design/decisions/declarative-collectors.md.
# Review before trusting; regenerate rather than hand-patch if the source collector changes.
#
@{
    ResourceTypes = @(
        'microsoft.insights/privatelinkscopes'
    )

    ResourceTypeMatching = 'Grouped'

    AdditionalFilter = $null

    FilterPreamble = ''

    RowLoopVariable = '1'

    Preamble = @'
$ResUCount = 1
            $sub1 = $SUB | Where-Object { $_.Id -eq $1.subscriptionId }
            $data = $1.PROPERTIES
            $Tags = if (![string]::IsNullOrEmpty($1.tags.psobject.properties)) { $1.tags.psobject.properties } else { '0' }

            $peLinkCount = if ($data.privateEndpointConnections) {
                @($data.privateEndpointConnections).Count
            } else { 0 }

            $scopedResCount = if ($data.scopedResources) {
                @($data.scopedResources).Count
            } else { 0 }

            $scopedResTypes = if ($data.scopedResources) {
                (@($data.scopedResources) | ForEach-Object {
                    $_.linkedResourceId -split '/' | Select-Object -Skip 6 -First 2 | Join-String -Separator '/'
                } | Sort-Object -Unique) -join '; '
            } else { 'None' }
'@

    AdditionalRowLoops = @()

    TagLoop = @{
        Variable = 'Tag'
        Source = '$Tags'
        Preamble = ''
    }

    Fields = @(
        @{
            Name = 'ID'
            Expression = '$1.id'
        }
        @{
            Name = 'Subscription'
            Expression = '$sub1.Name'
        }
        @{
            Name = 'Resource Group'
            Expression = '$1.RESOURCEGROUP'
        }
        @{
            Name = 'Private Link Scope Name'
            Expression = '$1.NAME'
        }
        @{
            Name = 'Location'
            Expression = '$1.LOCATION'
        }
        @{
            Name = 'Access Mode'
            Expression = 'if ($data.accessModeSettings.queryAccessMode) { $data.accessModeSettings.queryAccessMode } else { ''N/A'' }'
        }
        @{
            Name = 'Ingestion Access Mode'
            Expression = 'if ($data.accessModeSettings.ingestionAccessMode) { $data.accessModeSettings.ingestionAccessMode } else { ''N/A'' }'
        }
        @{
            Name = 'Private Endpoint Connections'
            Expression = '$peLinkCount'
        }
        @{
            Name = 'Scoped Resources Count'
            Expression = '$scopedResCount'
        }
        @{
            Name = 'Scoped Resource Types'
            Expression = '$scopedResTypes'
        }
        @{
            Name = 'Provisioning State'
            Expression = 'if ($data.provisioningState) { $data.provisioningState } else { ''N/A'' }'
        }
        @{
            Name = 'Resource U'
            Expression = '$ResUCount'
        }
        @{
            Name = 'Tag Name'
            Expression = '[string]$Tag.Name'
        }
        @{
            Name = 'Tag Value'
            Expression = '[string]$Tag.Value'
        }
    )

    Export = @{
        WorksheetName = 'Monitor Private Link Scopes'
        TableNamePrefix = 'MonPLSTable_'
        Columns = @(
            'Subscription'
            'Resource Group'
            'Private Link Scope Name'
            'Location'
            'Access Mode'
            'Ingestion Access Mode'
            'Private Endpoint Connections'
            'Scoped Resources Count'
            'Scoped Resource Types'
            'Provisioning State'
            'Resource U'
        )
        TagColumns = @()
        TagColumnsBefore = $null
        NumberFormat = '0'
        ConditionalText = @()
    }

    SourceCollector = 'Modules/Public/InventoryModules/Monitor/MonitorPrivateLinkScopes.ps1'
}

