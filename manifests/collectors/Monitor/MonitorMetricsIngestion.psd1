#
# GENERATED by scripts/ConvertTo-ScoutCollectorDefinition.ps1 from Modules/Public/InventoryModules/Monitor/MonitorMetricsIngestion.ps1 (AB#5660).
# Field expressions are copied verbatim from the original collector and evaluate in an
# equivalent scope -- see docs/design/decisions/declarative-collectors.md.
# Review before trusting; regenerate rather than hand-patch if the source collector changes.
#
@{
    ResourceTypes = @(
        'microsoft.operationalinsights/workspaces'
    )

    ResourceTypeMatching = 'Grouped'

    AdditionalFilter = $null

    FilterPreamble = ''

    RowLoopVariable = '1'

    Preamble = @'
$ResUCount = 1
            $sub1 = $SUB | Where-Object { $_.Id -eq $1.subscriptionId }
            $data = $1.PROPERTIES
            $Tags = if (![string]::IsNullOrEmpty($1.tags.psobject.properties)) { $1.tags.psobject.properties } else { '0' }

            # SKU details
            $skuName        = if ($data.sku.name)                           { $data.sku.name }                           else { 'N/A' }
            $capResLevel    = if ($data.sku.capacityReservationLevel)       { $data.sku.capacityReservationLevel }       else { 'N/A' }

            # Retention
            $retentionDays  = if ($data.retentionInDays)                    { $data.retentionInDays }                    else { 'N/A' }

            # Daily cap
            $dailyQuotaGb   = if ($data.workspaceCapping.dailyQuotaGb -and $data.workspaceCapping.dailyQuotaGb -ne -1) {
                                  $data.workspaceCapping.dailyQuotaGb
                               } else { 'Unlimited' }
            $dataIngestionStatus = if ($data.workspaceCapping.dataIngestionStatus) { $data.workspaceCapping.dataIngestionStatus } else { 'N/A' }

            # Customer-managed key
            $cmkEnabled     = if ($data.features.enableDataExport -eq $true) { 'Yes' } else { 'No' }

            # Public network access
            $publicIngestion = if ($data.publicNetworkAccessForIngestion)   { $data.publicNetworkAccessForIngestion }   else { 'N/A' }
            $publicQuery     = if ($data.publicNetworkAccessForQuery)       { $data.publicNetworkAccessForQuery }       else { 'N/A' }
'@

    AdditionalRowLoops = @()

    TagLoop = @{
        Variable = 'Tag'
        Source = '$Tags'
        Preamble = ''
    }

    Fields = @(
        @{
            Name = 'ID'
            Expression = '$1.id'
        }
        @{
            Name = 'Subscription'
            Expression = '$sub1.Name'
        }
        @{
            Name = 'Resource Group'
            Expression = '$1.RESOURCEGROUP'
        }
        @{
            Name = 'Workspace Name'
            Expression = '$1.NAME'
        }
        @{
            Name = 'Location'
            Expression = '$1.LOCATION'
        }
        @{
            Name = 'SKU'
            Expression = '$skuName'
        }
        @{
            Name = 'Capacity Reservation (GB/d)'
            Expression = '$capResLevel'
        }
        @{
            Name = 'Retention (days)'
            Expression = '$retentionDays'
        }
        @{
            Name = 'Daily Cap (GB)'
            Expression = '$dailyQuotaGb'
        }
        @{
            Name = 'Daily Cap Status'
            Expression = '$dataIngestionStatus'
        }
        @{
            Name = 'Public Ingestion'
            Expression = '$publicIngestion'
        }
        @{
            Name = 'Public Query'
            Expression = '$publicQuery'
        }
        @{
            Name = 'Provisioning State'
            Expression = 'if ($data.provisioningState) { $data.provisioningState } else { ''N/A'' }'
        }
        @{
            Name = 'Resource U'
            Expression = '$ResUCount'
        }
        @{
            Name = 'Tag Name'
            Expression = '[string]$Tag.Name'
        }
        @{
            Name = 'Tag Value'
            Expression = '[string]$Tag.Value'
        }
    )

    Export = @{
        WorksheetName = 'Monitor Metrics Ingestion'
        TableNamePrefix = 'MonitorMetricsIngTable_'
        Columns = @(
            'Subscription'
            'Resource Group'
            'Workspace Name'
            'Location'
            'SKU'
            'Capacity Reservation (GB/d)'
            'Retention (days)'
            'Daily Cap (GB)'
            'Daily Cap Status'
            'Public Ingestion'
            'Public Query'
            'Provisioning State'
            'Resource U'
        )
        TagColumns = @()
        TagColumnsBefore = $null
        NumberFormat = '0'
        ConditionalText = @(
            'New-ConditionalText -ConditionalType ContainsText  ''Unlimited'' -ConditionalTextColor ([System.Drawing.Color]::FromArgb(0,176,240)) -BackgroundColor ([System.Drawing.Color]::White)'
            'New-ConditionalText -ConditionalType ContainsText ''CapReached'' -ConditionalTextColor ([System.Drawing.Color]::White) -BackgroundColor ([System.Drawing.Color]::FromArgb(255,0,0))'
            'New-ConditionalText -ConditionalType ContainsText ''PerNodeNotAllowed'' -ConditionalTextColor ([System.Drawing.Color]::White) -BackgroundColor ([System.Drawing.Color]::FromArgb(255,165,0))'
        )
    }

    SourceCollector = 'Modules/Public/InventoryModules/Monitor/MonitorMetricsIngestion.ps1'
}

