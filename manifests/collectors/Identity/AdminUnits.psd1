#
# GENERATED by scripts/ConvertTo-ScoutCollectorDefinition.ps1 from Modules/Public/InventoryModules/Identity/AdminUnits.ps1 (AB#5660).
# Field expressions are copied verbatim from the original collector and evaluate in an
# equivalent scope -- see docs/design/decisions/declarative-collectors.md.
# Review before trusting; regenerate rather than hand-patch if the source collector changes.
#
@{
    ResourceTypes = @(
        'entra/administrativeunits'
    )

    ResourceTypeMatching = 'Grouped'

    AdditionalFilter = $null

    FilterPreamble = ''

    RowLoopVariable = '1'

    Preamble = @'
$ResUCount = 1
            $data = $1.properties

            $membershipType = if ($data.membershipType) { $data.membershipType } else { 'Assigned' }
            $membershipRule = if ($data.membershipRule) { $data.membershipRule } else { '' }
'@

    AdditionalRowLoops = @()

    TagLoop = $null

    Fields = @(
        @{
            Name = 'ID'
            Expression = '$1.id'
        }
        @{
            Name = 'Tenant ID'
            Expression = '$1.tenantId'
        }
        @{
            Name = 'Display Name'
            Expression = '$data.displayName'
        }
        @{
            Name = 'Description'
            Expression = '$data.description'
        }
        @{
            Name = 'Membership Type'
            Expression = '$membershipType'
        }
        @{
            Name = 'Membership Rule'
            Expression = '$membershipRule'
        }
        @{
            Name = 'Visibility'
            Expression = '$data.visibility'
        }
        @{
            Name = 'Resource U'
            Expression = '$ResUCount'
        }
    )

    Export = @{
        WorksheetName = 'Admin Units'
        TableNamePrefix = 'AdminUnitsTable_'
        Columns = @(
            'Display Name'
            'Description'
            'Membership Type'
            'Membership Rule'
            'Visibility'
            'Resource U'
        )
        TagColumns = @()
        TagColumnsBefore = $null
        NumberFormat = '0'
        ConditionalText = @()
    }

    SourceCollector = 'Modules/Public/InventoryModules/Identity/AdminUnits.ps1'
}

