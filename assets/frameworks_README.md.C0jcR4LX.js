import{_ as t,o as a,c as s,a2 as i}from"./chunks/framework.il0noOp3.js";const u=JSON.parse('{"title":"docs/frameworks — the standard for enumerated framework sources","description":"","frontmatter":{},"headers":[],"relativePath":"frameworks/README.md","filePath":"frameworks/README.md"}'),n={name:"frameworks/README.md"};function o(r,e,d,c,l,h){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="docs-frameworks-—-the-standard-for-enumerated-framework-sources" tabindex="-1">docs/frameworks — the standard for enumerated framework sources <a class="header-anchor" href="#docs-frameworks-—-the-standard-for-enumerated-framework-sources" aria-label="Permalink to &quot;docs/frameworks — the standard for enumerated framework sources&quot;">​</a></h1><p>This directory holds Scout&#39;s own enumeration of each framework a rule file scores against — CAF design areas, WAF pillars, SMART&#39;s subject areas, and any regulatory-compliance initiative added later. A rule file must never be written against a framework nobody enumerated first: that is how <code>waf.storage.yaml</code> ended up scoring a WAF pillar that does not exist (audit §8, item 9), and how a <code>caf.*</code> rule file could go on citing a recommendations heading Microsoft has since rewritten (audit §8, item 7). This file is the standard every enumeration file under this directory must meet. <code>smart-question-set.md</code> is the reference implementation — match its shape.</p><h2 id="why-this-exists-ab-6817" tabindex="-1">Why this exists (AB#6817) <a class="header-anchor" href="#why-this-exists-ab-6817" aria-label="Permalink to &quot;Why this exists (AB#6817)&quot;">​</a></h2><p>Two facts from the audit make framework currency a permanent, not a one-time, problem:</p><ol><li><strong>Microsoft is actively rewriting CAF design-area pages</strong> away from the &quot;Design considerations&quot; / &quot;Design recommendations&quot; structure. At least three pages already render as numbered task sections instead, with no recommendations heading at all. A coverage percentage computed against a recommendation count on one of those pages silently drifts the next time Microsoft edits it.</li><li><strong>Regulatory-compliance initiatives are versioned, and older versions stay live.</strong> Azure simultaneously ships six CIS Azure Foundations initiatives (v1.1.0 through v3.0.0), two ISO 27001 sets, three PCI sets, and both NIST SP 800-53 Rev. 5 and R5.1.1. &quot;CIS compliance: 72%&quot; names nothing if the reader cannot tell whether the denominator was 31 policies or 168.</li></ol><p>The fix is not a one-off correction — it is a rule that every enumeration file, and every coverage figure Scout ever prints, keeps naming what it was measured against.</p><h2 id="the-mandatory-header-block" tabindex="-1">The mandatory header block <a class="header-anchor" href="#the-mandatory-header-block" aria-label="Permalink to &quot;The mandatory header block&quot;">​</a></h2><p>Every file under <code>docs/frameworks/*.md</code> — except this <code>README.md</code> — must carry four pieces of information near the top, before the first content section: <strong>Source</strong>, <strong>Framework version</strong>, <strong>Extraction date</strong>, and <strong>Verification method</strong>. What actually landed in this directory while this standard was being written settled on two equally acceptable shapes rather than one; use whichever fits the file better, but do not invent a third:</p><p><strong>Shape A — a blockquote metadata block</strong>, immediately under the title (<code>smart-question-set.md</code>, <code>waf-pillar-checklists.md</code>, <code>caf-landing-zone-design-areas.md</code>):</p><div class="language-markdown vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;"># &lt;Framework name&gt; — &lt;what this file enumerates&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**Source:**</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;"> &lt;the canonical URL(s) this enumeration was read from&gt;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**Framework version:**</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;"> &lt;the framework&#39;s own version string, or, when the framework does not</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; publish one (CAF and WAF do not), a statement that the extraction date is being used as the</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; version&gt;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**Extracted:**</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;"> &lt;YYYY-MM-DD — the date the source was actually read&gt;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**Verification method:**</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;"> &lt;one or two sentences: what was read, how, and what could NOT be</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; confirmed&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><strong>Shape B — a <code>Field | Value</code> table</strong>, placed wherever the file&#39;s own structure reads best, paired with the <code>**Enumerated YYYY-MM-DD.**</code> lede paragraph most files already open with (<code>waf-ai-workload-checklist.md</code>, <code>waf-avd-workload-checklist.md</code>, <code>waf-avs-workload-checklist.md</code>, <code>waf-azure-local-checklist.md</code>):</p><div class="language-markdown vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**Enumerated YYYY-MM-DD.**</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ...</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| Field | Value |</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">|---|---|</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| Source page | &lt;URL(s)&gt; |</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| Framework version | &lt;version string, or &quot;not versioned by Microsoft — see extraction date&quot;&gt; |</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| Extraction date | YYYY-MM-DD |</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">| Verification method | &lt;what was read, how, and what could not be confirmed&gt; |</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>A third, older pattern — the <code>**Enumerated YYYY-MM-DD.**</code> lede plus a <code>## Verification method</code> section and a &quot;What was read&quot; table, but with no explicit <strong>Framework version</strong> line — predates this standard and is <strong>not sufficient on its own</strong>: it is missing the version field criterion 1 of AB#6817 requires. <code>avs-landing-zone-question-set.md</code>, <code>casa-question-set.md</code>, and <code>cloud-governance-question-set.md</code> used to be in this state; each now carries an added <strong><code>Framework version:</code></strong> line stating plainly that Microsoft does not version the source and that the extraction date is being used instead — add the same one line to any future file that starts from this pattern, rather than reworking its whole structure.</p><p><code>tests/FrameworkCurrency.Tests.ps1</code> parses all three shapes: it looks for a Source URL, a <code>Framework version</code> field or line, a parseable extraction date, and a <code>Verification method</code> field, section, or line, in any of the arrangements above. It does not care which shape a file uses — it cares that all four pieces of information are actually present.</p><p>After the header, the body is free-form Markdown. <code>smart-question-set.md</code> is the fullest template: a short &quot;What is this framework&quot; section, a longer &quot;Verification method&quot; section that states exactly what was read and what could not be (SMART&#39;s own question text is not published — the file says so rather than inventing it), and then the enumeration itself as one or more tables.</p><h2 id="the-id-scheme-convention" tabindex="-1">The ID-scheme convention <a class="header-anchor" href="#the-id-scheme-convention" aria-label="Permalink to &quot;The ID-scheme convention&quot;">​</a></h2><p>Every enumerated item gets a stable, citable identifier of the shape <code>&lt;PREFIX&gt;-&lt;SECTION&gt;&lt;N&gt;</code>, e.g. <code>SMART-A1</code>, <code>SMART-C6</code>. The prefix matches the <code>framework:</code> value used in the corresponding rule YAML file (<code>SMART</code>, <code>CAF</code>, <code>WAF</code>, and so on). These IDs are <strong>Scout&#39;s own</strong>, not Microsoft&#39;s — state that explicitly in the file if Microsoft does not publish numbered items of its own (CAF recommendations and SMART questions are both examples). A rule file must cite the enumeration file&#39;s ID in every rule&#39;s <code>remediation</code> text, so a reader can go from a failed rule straight back to the sentence in Microsoft&#39;s docs it is checking.</p><h2 id="no-coverage-percentage-without-a-named-version" tabindex="-1">No coverage percentage without a named version <a class="header-anchor" href="#no-coverage-percentage-without-a-named-version" aria-label="Permalink to &quot;No coverage percentage without a named version&quot;">​</a></h2><p><strong>Every coverage percentage Scout emits must name the framework version it was measured against.</strong> In the assessment engine this is carried as the <code>frameworkversion</code> key at the top of each rule YAML file (<code>src/assess/rules/*.yaml</code>), next to <code>framework:</code> and <code>area:</code>. It flows through unchanged:</p><ul><li><code>src/assess/engine/Get-RuleSet.ps1</code> reads <code>frameworkversion</code> off the YAML document and <strong>refuses to load any rule file that declares rules but no version</strong> — a load-time, fail-fast error, not a silent gap.</li><li><code>src/assess/Invoke-Assessment.ps1</code> stamps every finding it emits with the rule set&#39;s <code>FrameworkVersion</code>.</li><li><code>src/assess/engine/Get-Score.ps1</code> carries that version onto both the per-area and the per-framework roll-up (<code>Areas[].Version</code>, <code>Frameworks[].Version</code>) so any renderer consuming <code>Get-Score</code>&#39;s output has the version sitting next to the score it belongs to, not somewhere else the reader has to go find.</li></ul><p>For CAF and WAF, which Microsoft does not version, the <code>frameworkversion</code> string names the extraction/verification date instead — consistent with the header block above. For a versioned regulatory initiative (CIS, ISO, PCI, NIST — none exist as rule files yet), <code>frameworkversion</code> must name the actual initiative version (e.g. <code>CIS Azure Foundations v3.0.0</code>), not just a date, because the same initiative name legitimately means a different policy count release to release.</p><p><code>tests/FrameworkCurrency.Tests.ps1</code> is the gate: it asserts every loaded rule set carries a version, and proves the assertion is not vacuous by constructing a rule file with no <code>frameworkversion</code> and watching <code>Get-RuleSet</code> reject it.</p><h2 id="recheck-cadence-and-procedure" tabindex="-1">Recheck cadence and procedure <a class="header-anchor" href="#recheck-cadence-and-procedure" aria-label="Permalink to &quot;Recheck cadence and procedure&quot;">​</a></h2><p><strong>Cadence: every 90 days</strong>, or immediately whenever a rule file that depends on an enumeration is touched. Ninety days matches the pace the audit actually observed — Microsoft rewriting CAF pages and shipping new CIS/NIST initiative versions is not an annual event — while staying loose enough that a quarterly documentation pass, not a CI job, can carry it. <code>pmo/audits/AZURE-SCOUT-AUDIT.md</code> §8 is the log of what a full recheck has found before; keep adding to it rather than starting a parallel record.</p><p><strong>Procedure</strong>, per enumeration file:</p><ol><li>Re-open every URL in the file&#39;s <code>Source</code> field. Note anything that redirects, is marked archived, or has visibly changed structure (a missing &quot;Design recommendations&quot; heading, a renamed section, a retired taxonomy) — these are exactly the audit&#39;s §8 findings.</li><li>Re-count the enumerated items. If the count changed, update both the enumeration table and every rule file&#39;s <code>remediation</code> text that cites an ID whose meaning shifted.</li><li>Update the header block&#39;s <code>Extracted</code> date to today and, if anything in step 1 changed the substance of what was read, update <code>Verification method</code> to say so.</li><li>If the framework is a versioned regulatory initiative, confirm the initiative version named in <code>frameworkversion</code> is still current, and if Microsoft has shipped a newer version, decide explicitly whether Scout moves to it or keeps scoring the old one on purpose — do not let the version string go stale by default.</li><li>Re-run <code>tests/FrameworkCurrency.Tests.ps1</code>. It will fail the file&#39;s age check once it is sufficiently overdue (see the test file for the exact grace period and the reasoning for it) as a backstop for step 3 being skipped, not as the primary trigger for doing a recheck.</li></ol><h2 id="files-in-this-directory" tabindex="-1">Files in this directory <a class="header-anchor" href="#files-in-this-directory" aria-label="Permalink to &quot;Files in this directory&quot;">​</a></h2><table tabindex="0"><thead><tr><th>File</th><th>Framework</th><th>Enumerates</th></tr></thead><tbody><tr><td><code>smart-question-set.md</code></td><td>SMART</td><td>The published CAF Migrate content SMART assesses readiness against — the reference implementation of this standard</td></tr><tr><td><code>waf-pillar-checklists.md</code></td><td>WAF</td><td>All five WAF pillar design-review checklists, 59 items total, Microsoft&#39;s own item codes (<code>RE:01</code> etc.)</td></tr><tr><td><code>caf-landing-zone-design-areas.md</code></td><td>CAF</td><td>All eight CAF landing-zone design areas (393 items total); Network topology and connectivity (135 items, 11 of ~14 pages) still has ~3-4 unfetched child pages recorded as a residual gap</td></tr></tbody></table><p>Add a row here whenever a new enumeration file lands.</p>`,29)])])}const m=t(n,[["render",o]]);export{u as __pageData,m as default};
