import{_ as n,o as t,c as a,a2 as s}from"./chunks/framework.il0noOp3.js";const u=JSON.parse('{"title":"Multi-tenant sign-in and recovery","description":"","frontmatter":{},"headers":[],"relativePath":"how-to/multi-tenant-recovery.md","filePath":"how-to/multi-tenant-recovery.md"}'),i={name:"how-to/multi-tenant-recovery.md"};function r(o,e,c,l,d,p){return t(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="multi-tenant-sign-in-and-recovery" tabindex="-1">Multi-tenant sign-in and recovery <a class="header-anchor" href="#multi-tenant-sign-in-and-recovery" aria-label="Permalink to &quot;Multi-tenant sign-in and recovery&quot;">​</a></h1><p>Azure Scout authenticates the account at the start of a multi-tenant run, then acquires tenant contexts from that session. Selecting device-code login does not force another sign-in for each child. Children do not initiate interactive authentication. If a tenant requires a policy challenge or a new sign-in, its failure is recorded and the remaining tenants continue.</p><p>The guided menu asks whether to reuse the signed-in account, discovers accessible tenants, and offers one, selected, or all tenants. It asks for an individual tenant only after selecting the single-tenant option. Cancelling or selecting no tenants does not start a scan.</p><h2 id="recover-a-run" tabindex="-1">Recover a run <a class="header-anchor" href="#recover-a-run" aria-label="Permalink to &quot;Recover a run&quot;">​</a></h2><p>Use the umbrella directory containing <code>run-summary.json</code>:</p><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Retry failures, partial results and interrupted attempts only.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-AzureScout</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ResumeRun </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;D:\\Reports\\customer-run&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">RetryFailed</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Retry one specific tenant from that run.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-AzureScout</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ResumeRun </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;D:\\Reports\\customer-run&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">RetryTenant </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;&lt;tenant-id&gt;&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Continue pending, failed, partial and interrupted tenants.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-AzureScout</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ResumeRun </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;D:\\Reports\\customer-run&#39;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Resume uses the original account, module version and saved scan settings. Each retry writes a new attempt directory; successful tenant artifacts and previous attempts are preserved. A file lock prevents concurrent writers. The versioned checkpoint rejects unsupported schemas and changed scan settings.</p><p>Credentials are never saved in the checkpoint. Re-supply <code>-Secret</code>, <code>-CertificatePassword</code>, <code>-DevOpsPat</code> or <code>-OktaApiToken</code> when needed. Other scan options come from the checkpoint. For a tenant requiring authentication, sign in to that tenant with the original account, then retry it. Tenant consent and Conditional Access may require genuine interaction; Scout cannot bypass those requirements.</p><p>Older v1 run summaries cannot be resumed. Use a separate <code>Invoke-AzureScout -TenantID &#39;&lt;tenant-id&gt;&#39; -NoWizard</code> run with the original scan options for those runs.</p><h2 id="understand-completion" tabindex="-1">Understand completion <a class="header-anchor" href="#understand-completion" aria-label="Permalink to &quot;Understand completion&quot;">​</a></h2><p>A child is complete only when it returns a typed result for the requested tenant and its report or evidence artifacts exist inside that attempt directory. A nonthrowing child with no result is a failure. Partial collection remains Partial; it is not promoted to Completed. Root summaries include pending, running, completed, partial, interrupted, skipped and failed counts.</p><p>If a checkpoint write fails, execution stops rather than scanning additional tenants without recording progress. The previous atomic JSON checkpoint and attempt directories remain available. Restore filesystem access before resuming; an interrupted attempt is retried in a new directory.</p><h2 id="graph-and-guest-accounts" tabindex="-1">Graph and guest accounts <a class="header-anchor" href="#graph-and-guest-accounts" aria-label="Permalink to &quot;Graph and guest accounts&quot;">​</a></h2><p>Default Graph collection uses the selected Azure PowerShell identity. Scout also accepts an already-connected Microsoft Graph SDK context when its account, tenant, cloud and required scopes match. SDK context is checked on every token-provider lookup, including pagination and retries; a cached provider marker cannot select another tenant&#39;s active SDK context.</p><p>Scout does not automatically start a separate Graph device-code flow. For endpoints requiring additional delegated scopes, an administrator can explicitly consent and connect the Graph SDK before a targeted run. Azure role assignments, directory roles, OAuth scopes, consent, guest restrictions and licensing are independent requirements. Minimum ARM inventory permissions do not promise complete PIM, access-review or risky-user coverage. Entra Connect host topology can require local host access; Graph consent alone does not supply it.</p><p>The report includes catalog-derived identity permission requirements when an Identity cache exists. These describe requirements, not granted access. Collection outcomes record unavailable and not-assessed datasets. Supported authentication behavior is documented in <a href="https://learn.microsoft.com/en-us/powershell/module/az.accounts/set-azcontext" target="_blank" rel="noreferrer">Set-AzContext</a> and <a href="https://learn.microsoft.com/en-us/powershell/microsoftgraph/authentication-commands" target="_blank" rel="noreferrer">Microsoft Graph authentication</a>.</p><h2 id="evidence-integrity" tabindex="-1">Evidence integrity <a class="header-anchor" href="#evidence-integrity" aria-label="Permalink to &quot;Evidence integrity&quot;">​</a></h2><p>An inventory normalization failure no longer silently substitutes a narrower Resource Graph assessment. Scoring stops, while combined runs retain collected inventory and produce a partial inventory report. Processed datasets preserve child rows and label them separately from distinct resource identities. Raw identity evidence remains available even when ARM normalization fails.</p><p>Drift uses assessment membership plus rule ID. The same rule in different assessments can retain different verdicts. Existing bare-rule history cannot safely establish those assessment-specific baselines, so the first comparison after this change marks those entries New.</p><p>Customer-specific corrections are not hardcoded into future assessments. Historical verdict verification and compatibility with external customer export scripts require their original evidence/schema. Use the supported <code>-FromCollect</code> path to regenerate a report and its findings from a Scout collection snapshot; do not hand-edit HTML and leave companion JSONs stale.</p>`,20)])])}const m=n(i,[["render",r]]);export{u as __pageData,m as default};
