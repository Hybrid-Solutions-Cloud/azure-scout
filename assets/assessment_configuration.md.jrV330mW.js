import{_ as t,o as s,c as a,a2 as i}from"./chunks/framework.il0noOp3.js";const g=JSON.parse('{"title":"Configuration and report tiers","description":"Saving and loading an assessment configuration, and the report tiers each run can emit.","frontmatter":{"description":"Saving and loading an assessment configuration, and the report tiers each run can emit."},"headers":[],"relativePath":"assessment/configuration.md","filePath":"assessment/configuration.md"}'),n={name:"assessment/configuration.md"};function r(o,e,d,l,c,h){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="configuration-and-report-tiers" tabindex="-1">Configuration and report tiers <a class="header-anchor" href="#configuration-and-report-tiers" aria-label="Permalink to &quot;Configuration and report tiers&quot;">​</a></h1><p>How to pin an assessment&#39;s configuration so a run is reproducible, and what each output tier produces.</p><h2 id="assessment-config-load-save" tabindex="-1">Assessment config load/save <a class="header-anchor" href="#assessment-config-load-save" aria-label="Permalink to &quot;Assessment config load/save&quot;">​</a></h2><p><code>Import-ScoutConfig</code> / <code>Export-ScoutConfig</code> (AB#373–375) let you save and reload the effective assessment config — an alternative benchmark, rule-selection glob patterns, and per-rule threshold overrides — as a single JSON file, mirroring exactly what the engine already consumes (no new schema invented):</p><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Load a config (falls back to the built-in ALZ reference benchmark if the</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># file is absent, missing, or unparsable -- never throws)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$config </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Import-ScoutConfig</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ConfigPath .</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">my</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config.json</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Round-trip: save the effective config back out</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Export-ScoutConfig</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Config $config </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Path .</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">my</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config.json </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Force</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>Every key (<code>benchmark</code>, <code>rulePatterns</code>, <code>ruleOverrides</code>) is optional and independently overridable. A missing/invalid <code>-ConfigPath</code> degrades to &quot;run with defaults&quot; with a <code>Write-Warning</code> rather than aborting the assessment.</p><h2 id="report-tiers" tabindex="-1">Report tiers <a class="header-anchor" href="#report-tiers" aria-label="Permalink to &quot;Report tiers&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Tier</th><th>Output</th><th>Notes</th></tr></thead><tbody><tr><td>Power BI</td><td><code>powerbi/*.csv</code> + <code>.pbit</code></td><td>Primary analytics tier (star schema); the <code>.pbit</code> template is bound to the CSVs so it opens pre-wired in Power BI Desktop.</td></tr><tr><td>HTML</td><td><code>report.html</code></td><td>Self-contained, single file</td></tr><tr><td>PowerPoint</td><td><code>assessment_deck.pptx</code></td><td>Executive deck via the OpenXML SDK — <strong>no Python dependency</strong>. First use needs the <code>dotnet</code> SDK; see <a href="./assessment-prerequisites.html#powerpoint-tier-net-sdk-not-python">Assessment Prerequisites</a>.</td></tr><tr><td>Excel</td><td><code>assessment_evidence.xlsx</code></td><td>Evidence tier, plus pivot-chart visual dashboard tabs (Findings-by-Severity, Score-by-Area, Pass-Fail-Manual, Resource-Counts) generated with <code>ImportExcel</code> — each tab is omitted when its underlying data is empty.</td></tr><tr><td>JSON</td><td><code>findings.json</code></td><td>The machine-readable contract — full assessment metadata, scores, and findings.</td></tr><tr><td>JSON evidence</td><td><code>evidence.json</code> (<code>Export-JsonEvidence</code>)</td><td>Resources-only export of the raw <code>collect.json</code> data (<strong>AB#396</strong>) — no assessment metadata, scores, or findings. For callers that just want the discovered resources as JSON.</td></tr><tr><td>React</td><td><code>report-react.html</code></td><td>Self-contained (CSS/JS inline, findings embedded as a JSON blob, no external/CDN requests). A vis.js VNet topology diagram with click-to-details and reset/fit controls, an MG-hierarchy diagram, 14 KPI cards, an Azure Firewall drill-down, a Governance section (budgets/locks/tag chips), a policy-enforcement badge, per-section search/filter, clickable rows with a side panel, scope tooltips, client-side filter by Framework/Area/Severity/Status, a sortable/searchable findings table, and a Drift tab showing cross-run drift (see <a href="#cross-run-drift">Cross-run drift</a>).</td></tr><tr><td>Word</td><td><code>assessment_report.docx</code> (<code>Export-Word</code>)</td><td>Self-contained <code>.docx</code> via the OpenXML SDK — <strong>no Python dependency</strong>, same NuGet-on-first-use pattern as the PowerPoint tier (<strong>AB#333</strong>). Falls back to a plain HTML file (clearly labeled, not a renamed <code>.docx</code>) if generation fails.</td></tr><tr><td>ECharts dashboard</td><td><code>assessment_dashboard.html</code> (<code>Export-EChartsDashboard</code>)</td><td>Self-contained offline HTML dashboard — Apache ECharts is inlined into the file, no CDN/external requests (<strong>AB#344</strong>).</td></tr><tr><td>PDF</td><td><code>assessment_report.pdf</code> (<code>Export-Pdf</code>)</td><td>Hand-rolled, dependency-free PDF renderer — cover page, executive summary, per-area findings table with a repeating header, prioritized gaps, and the manual-review worklist (<strong>AB#379/394/395</strong>). Falls back to an HTML file with print-to-PDF instructions if generation fails.</td></tr></tbody></table>`,8)])])}const k=t(n,[["render",r]]);export{g as __pageData,k as default};
