import{_ as t,o,c as r,a2 as n}from"./chunks/framework.il0noOp3.js";const p=JSON.parse(`{"title":"Decision record — a deterministic inventory processing pipeline","description":"Decision record — replace the inventory processing phase's background-job and runspace orchestration with a single deterministic in-process pipeline. Delivers AB#5651 under Feature AB#5649.","frontmatter":{"description":"Decision record — replace the inventory processing phase's background-job and runspace orchestration with a single deterministic in-process pipeline. Delivers AB#5651 under Feature AB#5649."},"headers":[],"relativePath":"design/decisions/deterministic-pipeline.md","filePath":"design/decisions/deterministic-pipeline.md"}`),i={name:"design/decisions/deterministic-pipeline.md"};function a(s,e,d,c,l,h){return o(),r("div",null,[...e[0]||(e[0]=[n('<h1 id="decision-record-—-a-deterministic-inventory-processing-pipeline" tabindex="-1">Decision record — a deterministic inventory processing pipeline <a class="header-anchor" href="#decision-record-—-a-deterministic-inventory-processing-pipeline" aria-label="Permalink to &quot;Decision record — a deterministic inventory processing pipeline&quot;">​</a></h1><blockquote><p><strong>Status:</strong> Accepted 2026-07-25. Approved as part of the engine rebuild (Epic <strong>AB#5638</strong>), which the repo owner authorised in full.</p><p><strong>Date:</strong> 2026-07-25 · <strong>Delivers:</strong> AB#5651 · <strong>Feature:</strong> AB#5649 · <strong>Epic:</strong> AB#5638</p></blockquote><h2 id="_1-decision" tabindex="-1">1. Decision <a class="header-anchor" href="#_1-decision" aria-label="Permalink to &quot;1. Decision&quot;">​</a></h2><p><strong>Run every inventory collector in-process, sequentially, in a fixed order. Use no background jobs and no nested runspaces in the processing phase.</strong></p><p>Concretely: <code>Start-AZSCProcessJob</code>, <code>Start-AZSCAutProcessJob</code>, <code>Wait-AZSCJob</code> (for resources) and <code>Build-AZSCCacheFiles</code> are replaced by four functions under <code>src/pipeline/</code>:</p><table tabindex="0"><thead><tr><th>Function</th><th>Responsibility</th></tr></thead><tbody><tr><td><code>Get-ScoutCollector</code></td><td>Discover collectors — a pure function of the filesystem plus the category filter</td></tr><tr><td><code>Invoke-ScoutCollector</code></td><td>Run <strong>one</strong> collector, contain its failure, time it</td></tr><tr><td><code>Invoke-ScoutProcessing</code></td><td>Run them all in order, group by category, write the cache</td></tr><tr><td><code>Write-ScoutCacheFile</code></td><td>Write one category&#39;s rows — takes <strong>data</strong>, not job names</td></tr></tbody></table><h2 id="_2-why-—-the-old-design-s-failures-were-structural-not-incidental" tabindex="-1">2. Why — the old design&#39;s failures were structural, not incidental <a class="header-anchor" href="#_2-why-—-the-old-design-s-failures-were-structural-not-incidental" aria-label="Permalink to &quot;2. Why — the old design&#39;s failures were structural, not incidental&quot;">​</a></h2><p>The forked engine created one <code>Start-Job</code> per category, and each of those jobs created one <code>[PowerShell]::Create()</code> runspace per collector. Every defect of the v2.5.x wave lived in that coordination layer rather than in the collectors:</p><ul><li><strong><code>Start-Job</code> is asynchronous.</strong> A job created moments earlier sits in <code>NotStarted</code>. The wait condition tested <code>State -eq &#39;Running&#39;</code>, so such a job was never waited on — then <code>Receive-Job</code> harvested it empty and <code>Remove-Job</code> destroyed it. A whole category vanished from the report with no error anywhere. This is AB#5629, and it is why <code>ReportCache/Compute.json</code> came back 5,158 bytes on one run and 470 on the next against the same tenant.</li><li><strong>The inner wait was a no-op.</strong> <code>BeginInvoke()</code> returns a <code>PowerShellAsyncResult</code>, which has no <code>.Runspace</code> property. <code>While ($Job.Runspace.IsCompleted -contains $false)</code> therefore evaluated an empty collection, <code>-contains $false</code> was always false, and <code>EndInvoke</code> raced its own work. v2.5.2 fixed one of the two copies of this line; the other survived to v2.5.3.</li><li><strong>Each job re-imported the module.</strong> That re-applied module-scope StrictMode <em>inside</em> the job even when the calling orchestrator had opted out — which is why the v2.5.3 opt-out had to be applied at <strong>17</strong> entry points instead of 5, one per job body.</li><li><strong>Ordering came from <code>Get-Job</code>.</strong> Two runs over an unchanged estate could process categories in different orders and produce different output.</li><li><strong>Errors surfaced detached.</strong> An exception inside a runspace appeared at <code>EndInvoke</code> time, separated from the collector that raised it, so one bad collector silently emptied its whole category. Two collectors have been dead in every shipped release for exactly this reason (see §5).</li><li><strong>The estate was serialised per category.</strong> <code>ConvertTo-Json -Depth 40</code> over the full resource set, once for each of the 16 category jobs, then <code>ConvertFrom-Json</code> inside each.</li></ul><p>Each of these was individually patchable, and several <em>were</em> patched. The pattern is that fixing one exposed the next: v2.5.3 alone fixed six stacked crashes, each reachable only once the previous cleared, at roughly seven minutes per live run to discover each one. The coordination layer was the defect source, so removing it removes the class.</p><h2 id="_3-why-sequential-is-not-a-performance-regression" tabindex="-1">3. Why sequential is not a performance regression <a class="header-anchor" href="#_3-why-sequential-is-not-a-performance-regression" aria-label="Permalink to &quot;3. Why sequential is not a performance regression&quot;">​</a></h2><p>The obvious objection is that this trades parallelism for determinism. It does not, because the parallelism was largely illusory:</p><ul><li>Collectors are <strong>pure CPU-bound filters over an in-memory array</strong> — <code>$Resources | Where-Object { $_.TYPE -eq &#39;…&#39; }</code> and row shaping. They make no network calls; all Azure I/O happens in the extraction phase, before this one.</li><li><code>Start-Job</code> is <strong>out-of-process</strong>. Every argument crosses a serialisation boundary. The old design paid a full JSON round-trip of the entire estate per category to buy concurrency for work that is a few hundred milliseconds of array filtering.</li></ul><p>A smoke run over the real 176 collectors completes in well under two minutes on a synthetic estate, with the serialisation cost gone entirely. If a genuinely large estate ever makes this phase the bottleneck, <code>ForEach-Object -Parallel</code> (in-process, PowerShell 7) is available without reintroducing job harvesting — but it must not be adopted speculatively, because determinism is the property this work exists to deliver.</p><h2 id="_4-resilience-is-better-not-worse" tabindex="-1">4. Resilience is better, not worse <a class="header-anchor" href="#_4-resilience-is-better-not-worse" aria-label="Permalink to &quot;4. Resilience is better, not worse&quot;">​</a></h2><p>Determinism was not bought with fragility. <code>Invoke-ScoutCollector</code> contains each collector&#39;s failure individually: a collector that throws is reported and skipped, every other collector still runs, and the report is still produced. <code>Invoke-ScoutProcessing</code> returns the failure list so the run log and the console can state plainly what did not run.</p><p>Under the old pipeline the same throw either emptied the category silently or took down the batch. &quot;The run completed&quot; now means something it did not mean before.</p><h2 id="_5-consequences" tabindex="-1">5. Consequences <a class="header-anchor" href="#_5-consequences" aria-label="Permalink to &quot;5. Consequences&quot;">​</a></h2><ul><li><strong>The <code>-Automation</code> processing branch is deleted.</strong> It existed only to substitute <code>Start-ThreadJob</code> where <code>Start-Job</code> was unavailable. With no jobs, automation and interactive runs execute identical code and can no longer drift — as the two collector-discovery implementations already had (only one applied per-file category filtering).</li><li><strong><code>-Heavy</code> no longer affects this phase.</strong> It only sized the parallel job batch. Peak memory is now one category at a time regardless. It still applies to extraction.</li><li><strong>Ordering is part of the contract</strong>, pinned by a test that hashes the cache files across two runs of the same input.</li><li><strong>Three real defects surfaced immediately</strong> once collectors ran in-process, all of them pre-existing and all previously masked by the job boundary: <ol><li><code>Start-AZSCSecCenterJob</code> was receiving <code>$null</code> instead of the security rows, because <code>Invoke-AZSCSecurityCenterJob</code> was called with a parameter its own param block did not declare — and PowerShell routes an unknown named argument on a simple function into <code>$args</code> rather than rejecting it. <strong>The Security Center sheet was empty in every release that had one.</strong></li><li>Five collectors called <code>Write-AZSCLog -Color</code>/<code>-Level Verbose</code>, neither of which the function accepted, so each threw on its first log line and produced nothing.</li><li><code>Identity/IdentityProviders</code> and <code>Identity/SecurityDefaults</code> are written against a registration API that exists only as a <strong>mock inside the test suite</strong> and was never implemented. They have never produced a row. They are now detected at discovery and reported as skipped rather than executed, so a genuine failure is not buried under two guaranteed ones. Porting them is tracked under AB#5656.</li></ol></li></ul><h2 id="_6-what-this-deliberately-does-not-do" tabindex="-1">6. What this deliberately does not do <a class="header-anchor" href="#_6-what-this-deliberately-does-not-do" aria-label="Permalink to &quot;6. What this deliberately does not do&quot;">​</a></h2><ul><li><strong>The draw.io diagram subsystem still uses background jobs</strong>, and <code>Wait-AZSCJob</code> survives to serve it. That subsystem starts nested jobs of its own and is a separate piece of work; folding it in here would have made this change untestable in one step.</li><li><strong>StrictMode is still off for collectors.</strong> They were written for a runspace where it never applied, and turning it on needs the recorded live-payload fixtures that AB#5667 covers. Doing both at once would be a flag day — exactly what the rebuild plan rules out.</li></ul><h2 id="_7-alternatives-rejected" tabindex="-1">7. Alternatives rejected <a class="header-anchor" href="#_7-alternatives-rejected" aria-label="Permalink to &quot;7. Alternatives rejected&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Alternative</th><th>Why not</th></tr></thead><tbody><tr><td>Keep the jobs, fix the waits again</td><td>Six stacked crashes in v2.5.3 alone, each found by a seven-minute live run. Patching the coordination layer had already been tried repeatedly; the next tenant-specific variant was always waiting.</td></tr><tr><td><code>ForEach-Object -Parallel</code></td><td>In-process, so it avoids the serialisation cost, but reintroduces non-deterministic ordering and interleaved failure reporting for work that does not need concurrency. Available later if profiling ever justifies it.</td></tr><tr><td>Thread jobs everywhere</td><td>Same harvesting and ordering problems as <code>Start-Job</code>, minus only the process boundary.</td></tr><tr><td>Rewrite the collectors at the same time</td><td>The 176 collectors are effectively data and are not the crash source. Changing the engine and its inputs together would leave no way to attribute a regression. That work is AB#5656.</td></tr></tbody></table>',23)])])}const g=t(i,[["render",a]]);export{p as __pageData,g as default};
